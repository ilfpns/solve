#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
	string name;
	getline(cin, name);

	cout << name << "??!" << endl;

	return 0;
}